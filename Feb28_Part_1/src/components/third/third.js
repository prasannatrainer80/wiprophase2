const Third = (props) => {
    return(
        <div>
            First Name : {props.firstName} <br/>
            Lats Name : {props.lastName} <br/>
            Company : {props.company}
        </div>
    )
}

export default Third;